/**
 * Diese Datei wurde durch __tests__/exportPoster.test.ts ersetzt.
 * Die alten Tests testeten die html2canvas-basierte API, die durch SVG-Export
 * ersetzt wurde. Neue Tests befinden sich in __tests__/exportPoster.test.ts.
 */

describe('exportPoster (legacy stub)', () => {
  it('redirected to __tests__/exportPoster.test.ts', () => {
    expect(true).toBe(true);
  });
});
